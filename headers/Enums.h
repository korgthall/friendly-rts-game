#ifndef ENUMS_H
#define ENUMS_H
#define SIZE 50

enum class BuildingType {
    HUB,
    BARRACKS,
    GLORBSTRACTOR
};

enum class UnitType {
    WORKER,
    SOLDIER
};

enum class ResourceType {
    GOLD,
    GLORBIUM
};

#endif
